export type EditFormProps = {
    population: number,
    capital: string,
    countryName: string
}

export type CountryInfoProps = {
    region: string,
    subregion: string,
    timezone: string,
    continent: string
}